# การจัดการไฟล์และไดเรกทอรี (File & Directory)
```
ls -lha
```
คำอธิบาย: ดูไฟล์และโฟลเดอร์ทั้งหมด (all) ในรูปแบบรายการ (long) ที่คนอ่านง่าย (human-readable) (จะแสดงขนาดไฟล์เป็น KB/MB/GB และแสดงไฟล์ที่ซ่อนอยู่ .)
```
cd <path>
```
คำอธิบาย: "Change Directory" หรือ "ย้าย" เข้าไปในโฟลเดอร์
ตัวอย่าง: cd /etc/nginx/sites-available
Pro-Tip: cd .. (กลับขึ้นไป 1 ชั้น), cd ~ (กลับไปที่ Home)
```
 pwd
```
คำอธิบาย: "Print Working Directory" ➔ บอกว่า "ตอนนี้คุณอยู่ที่โฟลเดอร์ไหน"
```
cp -r <source> <destination>
```
คำอธิบาย: "Copy" (คัดลอก) ➔ -r (recursive) จำเป็นมากสำหรับการคัดลอก "ทั้งโฟลเดอร์"
ตัวอย่าง: cp -r ~/mlflow_artifacts /mnt/backup/
```
 mv <old_name> <new_name>
```
คำอธิบาย: "Move" (ย้าย) หรือ "Rename" (เปลี่ยนชื่อ)
ตัวอย่าง (เปลี่ยนชื่อ): mv config.txt config.bak
```
rm <file>
```
คำอธิบาย: "Remove" (ลบ) ไฟล์
```
rm -r <folder>
```
คำอธิบาย: ลบ "ทั้งโฟลเดอร์" (recursive)

🚨 ระวัง!: rm -rf <folder> ➔ -f (force) ➔ คำสั่งนี้จะ "ลบ" ทุกอย่างโดย ไม่ถาม (เป็นคำสั่งที่อันตรายที่สุดใน Linux)
```
 mkdir -p <path/to/new_folder>
```
คำอธิบาย: "Make Directory" (สร้างโฟลเดอร์) ➔ -p (parents) จะสร้าง "โฟลเดอร์แม่" ให้ด้วย ถ้ายังไม่มี (เช่น สร้าง b ใน a/b แม้ a ยังไม่มี)
```
nano <file>
```
คำอธิบาย: "แก้ไข" ไฟล์ (Text Editor ที่ง่ายที่สุด)
วิธีใช้: Ctrl+O (Save/Write Out), Enter (ยืนยัน), Ctrl+X (Exit)
```
cat <file>
```
คำอธิบาย: "อ่าน" เนื้อหาทั้งหมดในไฟล์ออกมาโชว์ใน Terminal (เหมาะสำหรับไฟล์สั้นๆ)
```
touch <file>
```
คำอธิบาย: "สร้าง" ไฟล์เปล่าๆ (Empty file)